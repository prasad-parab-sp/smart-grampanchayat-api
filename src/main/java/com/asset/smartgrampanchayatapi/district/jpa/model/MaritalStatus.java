package com.asset.smartgrampanchayatapi.district.jpa.model;

/** Mirrors PostgreSQL enum {@code marital_status}. */
public enum MaritalStatus {
    single,
    married,
    widowed,
    divorced,
    separated
}
