package com.asset.smartgrampanchayatapi.district.jpa.model;

/**
 * Stored in {@code certificate_document_format.format_kind}; mirrors admin UI document categories.
 */
public enum DocumentFormatKind {
    DAKHALA,
    BILL,
    RECEIPT,
    NOTICE,
    OTHER
}
