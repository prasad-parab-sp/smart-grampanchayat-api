package com.asset.smartgrampanchayatapi.district.jpa.model;

/**
 * UI / catalog grouping for {@link CertificateType} (stored as VARCHAR in PostgreSQL).
 */
public enum CertificateTypeCategory {
    CERTIFICATE,
    REGISTRATION,
    PERMISSIONS,
    OTHERS
}
